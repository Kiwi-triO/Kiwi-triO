#include <iostream>
using namespace std;

int gcd(int a, int b) {
	while (a != b) {
		if (b > a) swap(a, b);
		a -= b;
	}
	return a;
}

int main() {
	int n; cin >> n;
	int a; cin >> a;
	int b;
	for (int i = 0; i < n - 1; i++) {
		cin >> b;
		a = gcd(a, b);
	}
	cout << a;
}
